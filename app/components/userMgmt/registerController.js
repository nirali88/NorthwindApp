define("registerController", ['app'], function (app) {
    "use strict";

    app.register.controller('registerController', ['$rootScope', registerController]);

    function registerController($rootScope) {

        var vm = this;
        
    }
});
